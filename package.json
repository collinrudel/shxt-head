{
  "name": "shxthead",
  "private": true,
  "workspaces": ["shared", "client", "server"],
  "scripts": {
    "dev": "npm run build -w shxthead-shared && concurrently \"npm run dev -w shxthead-server\" \"npm run dev -w shxthead-client\"",
    "build": "npm run build -w shxthead-shared && npm run build -w shxthead-server && npm run build -w shxthead-client",
    "start": "npm run start -w shxthead-server"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
